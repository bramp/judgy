# lib/providers/

State management layer (Riverpod providers).

Bridges the engine and services to the UI by exposing reactive state. Keep
providers thin — business logic belongs in the engine or services.

- **level_provider** — Current level selection and puzzle state.
- **theme_provider** — Active visual theme.
