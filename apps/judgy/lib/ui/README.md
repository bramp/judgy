# lib/ui/

Flutter widgets and screens.

All visual presentation lives here — screens, reusable widgets, themes, and
visual effects. UI code should read state from providers and delegate logic to
the engine; avoid putting game rules or persistence here.
